import {defineConfig} from 'vite';import vue from '@vitejs/plugin-vue';import {VitePWA} from 'vite-plugin-pwa';import {sites} from '@openai/sites-vite-plugin';
export default defineConfig({base:process.env.GITHUB_ACTIONS?'/game-flags/':'/',plugins:[vue(),sites(),VitePWA({registerType:'autoUpdate',includeAssets:['favicon.svg'],manifest:{name:'Flag Streak',short_name:'Flag Streak',description:'Quanto conosci le bandiere del mondo?',theme_color:'#08152f',background_color:'#08152f',display:'standalone',orientation:'portrait',start_url:'./?app=v3',scope:'./',icons:[{src:'icon.svg',sizes:'any',type:'image/svg+xml',purpose:'any maskable'}]},workbox:{cleanupOutdatedCaches:true,clientsClaim:true,skipWaiting:true,navigateFallback:'index.html',globPatterns:['**/*.{js,css,html,svg,woff2}'],runtimeCaching:[{urlPattern:({request})=>request.mode==='navigate',handler:'NetworkFirst',options:{cacheName:'flagstreak-pages-v3',networkTimeoutSeconds:3,expiration:{maxEntries:4,maxAgeSeconds:86400}}}]}})]})
